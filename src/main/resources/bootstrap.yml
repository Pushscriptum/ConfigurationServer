spring:
  application:
    name: config-server
  profiles:
    active:
      -native, git
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
        git:
          uri: https://github.com/Pushscriptum/ConfigurationServer.git
          search-paths: licensingservice
          password: (5K0Uw!g'
          username: pushscriptum@mail.ru